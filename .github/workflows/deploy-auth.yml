name: deploy-auth

on: 
  push:
    branches: 
      - master 
    paths:
      - 'auth/**'
      
jobs:
  build:
    runs-on: ububtu-latest
    steps:
      - uses: actions/checkoutv2
      - run: cd auth && docker build -t namanicebed/auth .
      - run: docker login -u $DOCKER_ID -p $DOCKER_PASSWORD
        env:
          DOCKER_ID: ${{ secrets.DOCKER_ID }}
          DOCKER_PASSWORD: ${{secrets.DOCKER_PASSWORD}}
      - run: docker push namanicebed/auth
            
    
